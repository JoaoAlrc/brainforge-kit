# Contextual rules for Codex

Rule caches are adapted from the Claude set, whose source is preserved. Do not
assume `.codex/rules` autoloads: use this index and read only rules covering task
paths. Verify the shared canonical source when a cache is stale. Operational
model/tool rules are defined in COORDINATION/MODELS.

No Claude rule cache was found. Use AGENTS and applicable local instructions.
